kind: Service
apiVersion: v1
metadata:
  name: projser
  labels:
    proj: projectone
spec:
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 31200
  selector:
    proj: projectone
  type: LoadBalancer
